import styled from "styled-components";

export const PictureFormCss = styled.div`

.add-image {
    position: fixed;
    bottom: 0;
    left: 0;
    display: flex;
    flex-direction: column;

    trasform: translate(5%, 5%);
}
`